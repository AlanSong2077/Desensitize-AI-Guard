{
  "id": "data-guard",
  "name": "Data Guard",
  "version": "2.3.0",
  "description": "四层数据脱敏插件 + 可逆加密模式：① HTTP 代理层——在请求发往 AI 前自动脱敏 messages 中的敏感信息；② 文件工具层——在 AI 读取 CSV/XLSX/XLS/DOCX/PPTX/PDF 文件前按列名精准脱敏；③ Python exec 层——拦截 exec/process 工具中的 Python 文件读取操作；④ Shell exec 层——拦截 50+ Shell/多语言命令；⑤ 可逆加密模式——AES-256-GCM 加密敏感数据，LLM 返回后解密还原。安装时自动清除旧版 hook，防止多版本冲突。",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "port": {
        "type": "integer",
        "description": "HTTP 代理监听端口，默认 47291",
        "default": 47291,
        "minimum": 1024,
        "maximum": 65535
      },
      "blockOnFailure": {
        "type": "boolean",
        "description": "脱敏脚本失败时是否阻断请求（true=阻断，false=透传原文），默认 true",
        "default": true
      },
      "fileGuard": {
        "type": "boolean",
        "description": "是否启用文件工具层脱敏（read/read_file/read_many_files 工具，CSV/XLSX/XLS），默认 true",
        "default": true
      },
      "pythonGuard": {
        "type": "boolean",
        "description": "是否启用 Python exec 层脱敏（exec/process 工具中的 Python 文件读取），默认 true",
        "default": true
      },
      "skipPrefix": {
        "type": "string",
        "description": "消息开头加此前缀可跳过消息文本脱敏，默认 [skip-guard]",
        "default": "[skip-guard]"
      }
    }
  },
  "uiHints": {
    "port": {
      "label": "HTTP 代理监听端口",
      "placeholder": "47291"
    },
    "blockOnFailure": {
      "label": "脱敏失败时阻断请求"
    },
    "fileGuard": {
      "label": "启用文件工具层脱敏（read 工具，CSV/XLSX/XLS）"
    },
    "pythonGuard": {
      "label": "启用 Python exec 层脱敏（exec/process 工具）"
    },
    "skipPrefix": {
      "label": "跳过脱敏的消息前缀",
      "placeholder": "[skip-guard]"
    }
  }
}
